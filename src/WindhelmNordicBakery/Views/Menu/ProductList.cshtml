@model MenuProductsViewModel

@{
    ViewData["Title"] = "Products";
}

<div class="container">
<h1>@Model.CurrentCategory</h1>
@foreach (var product in Model.Products)
{
    <partial name="_ProductCard" model="product" />
}
</div>